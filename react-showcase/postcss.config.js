// Minimal PostCSS config for React showcase
// This prevents Vite from using the parent Laravel project's PostCSS config
export default {
  plugins: {
    // No plugins needed - we're using plain CSS
  },
}
